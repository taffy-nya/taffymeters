[package]
name = "taffymeters"
version = "0.1.2"
edition = "2024"

[dependencies]
taffymeters-core = { path = "../core" }
ringbuf = "0.4.8"
eframe = "0.34.1"
image = "0.25.10"

[build-dependencies]
embed-resource = "3.0.8"
